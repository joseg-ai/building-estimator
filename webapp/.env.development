# VITE_API_URL is intentionally unset in dev — Vite's proxy in vite.config.ts
# forwards all /api/* requests to http://localhost:3001.
# Set this only for non-proxy deployments (e.g. pointing at a remote staging server).
# VITE_API_URL=http://localhost:3001/api
