# Orchestration Log: Linus — Phase 1 Round 1

**Date:** 2026-05-10T19:31:41Z  
**Agent:** Linus  
**Role:** Frontend wiring & integration planning

## Why

Once Rusty's API contract lands, Linus must wire the webapp to fetch from server instead of bundled defaults. This requires changes to context, pages, API client, and storage layers. Mapping these upfront avoids rework and merge conflicts.

## Mode

Background (Haiku). Async, frontend integration planning + wiring map authoring.

## Files Authored

- `.squad/decisions/inbox/linus-phase1-wiring-map.md` — Complete integration map covering API client functions to add, context state changes, PriceListPage UI updates (version display, "Save as new version" button), quote save flow changes, offline fallback strategy, and implementation checklist.

## Outcome

✅ **COMPLETE** — Wiring map finalized and merged to decisions.md. No code written yet (deferred to implementation phase).

- 4 new API client functions spec'd: apiGetActivePriceList, apiPostPriceListVersion, apiUpdatePriceListItem, apiActivatePriceListVersion.
- Context changes documented: 3 new state fields (activePriceListVersionId, priceListLoadedAt, catalogLoadedAt).
- PriceListPage UI plan: version display (read-only in Phase 1), "Save as new version" button.
- Optimistic PUT pattern defined for inline price edits.
- Quote save flow extended to include priceListVersionId at snapshot time.
- Offline fallback to bundled defaults via localStorage cache (fallback key: 'cached-price-list-version').
- 12-item implementation checklist provided for code phase.

## Risks Identified

- Context reducer may become fat if Phase 2/3 add customers/vendors (mitigation: revisit in Phase 2, consider Zustand).
- Quote pinning price list version has no UI warning if user saves, then prices change (mitigation: deferred to Phase 2 badge UI).
- No version history UI in Phase 1 (mitigation: deferred to Phase 2 dropdown).
- Optimistic updates + cache invalidation ignored in MVP (mitigation: "refresh" button can be added in Phase 2).

## Next Steps

- Implement 4 API client functions per spec.
- Wire context initialization on app mount.
- Update PriceListPage UI and handlers.
- Test offline fallback and optimistic PUT.
