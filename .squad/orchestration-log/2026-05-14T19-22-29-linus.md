# Orchestration Log: Linus

**Date:** 2026-05-14T19:22:29-05:00  
**Agent:** Linus (claude-sonnet-4.6)  
**Mode:** background  
**Runtime:** 374s

## Assignment

UI/API fixes — login register removed, customer form (Company Name mandatory, company removed), quotes page blank fix.

## Deliverables

### LoginPage.tsx
- Removed Sign In / Register tab bar
- Removed Display Name field
- Removed `mode` state and `register` branch from `handleSubmit`

### CustomersPage.tsx
- Removed `company` from `FormData` interface, `emptyForm()`, `customerToForm()`, `formToWritable()`
- Renamed "Customer Name" → "Company Name" label (underlying `name` field unchanged)
- Added HTML `required` attribute to Company Name input
- Disabled submit button when Company Name is empty
- Removed "Company" column from table display

### QuotesPage.tsx & api.ts
- Fixed snake_case → camelCase mapping in `apiListQuotes()`
- Added defensive guards: `formatUSD(q.grandTotal ?? 0)`, `q.updatedAt?.toLocaleDateString()`

## Verification

`cd webapp && npm run build` → exit 0, 54 modules, no TypeScript errors.

## Status

✅ COMPLETE
