# Orchestration Log: Rusty

**Date:** 2026-05-14T19:22:29-05:00  
**Agent:** Rusty (claude-sonnet-4.6)  
**Mode:** background  
**Runtime:** 692s

## Assignment

Customers 500 fix — phase 2/3 table bootstrap, global error middleware, customerId wiring, vendor_prices UNIQUE constraint.

## Root Causes Fixed

1. **db.js:** Missing `CREATE TABLE IF NOT EXISTS` for phase 2/3 tables
2. **index.js:** No global error middleware (HTML 500 instead of JSON)
3. **routes-quotes.js:** `customerId` not wired in POST/PUT/GET
4. **vendor_prices DDL:** Missing `UNIQUE(vendor_id, item_key)` constraint

## Deliverables

### db.js
- Added full schema DDL for: `customers`, `vendors`, `vendor_prices`, `price_list_versions`, `material_prices`, `materials_catalog_*`
- Fixed vendor_prices UNIQUE constraint

### index.js
- Added global error middleware (always last):
  ```js
  app.use((err, _req, res, _next) => { ... });
  ```

### routes-quotes.js
- POST: validates `customerId` ownership (400 INVALID_CUSTOMER)
- PUT: accepts `customerId` (null or new id)
- GET list: accepts `?customerId=N` filter
- GET /:id: returns `customerId` and `priceListVersionId`

## Verification

`node --test test/*.test.js` → **74 pass, 0 fail** (was 60/14)  
POST /api/customers `{ name: "Acme Corp" }` → 201

## Status

✅ COMPLETE
