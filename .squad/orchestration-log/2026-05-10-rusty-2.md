# Rusty — Phase 2 (Customers API)

**Date:** 2026-05-10  
**Task:** Customers table + 6 CRUD routes + quote↔customer linking

## Outcome: ✅ SHIPPED

### Deliverables
- ✅ `customers` table schema (id, user_id, name, company, contact_name, email, phone, address_*, notes, default_labor_rate, default_overhead_pct, default_profit_pct, default_commission_pct, created_at, updated_at, indexes)
- ✅ Quotes table: added `customer_id INTEGER FK → customers(id) ON DELETE SET NULL`
- ✅ 6 routes implemented: GET /api/customers, GET /api/customers/:id, POST, PUT, DELETE, GET /:id/quotes
- ✅ Error code contract: VALIDATION, NOT_FOUND, IN_USE, INVALID_CUSTOMER
- ✅ Owner scoping: queries filter by current user; 404 returns on foreign records
- ✅ Quote integration: POST/PUT /api/quotes accept customerId, GET routes return it, DELETE cascade works

### Test Results
- 16 new customer endpoint tests: **all passing**
- 5 quote integration tests: **all passing**
- 25 prior catalog/pricelist tests: **all passing**
- **Total: 46/46 green**

### Known Issues
- **BUG-1 (Minor):** Non-integer customerId returns error code `VALIDATION` instead of `INVALID_CUSTOMER`. HTTP 400 is correct; only enum is wrong. Rusty fixing in parallel.

---
**Phase Status:** Ready for production. Handoff complete to Linus (frontend) and Saul (test verification).
