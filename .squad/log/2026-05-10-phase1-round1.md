# Phase 1 Round 1 — Session Log

**Date:** 2026-05-10  
**Phase:** Phase 1 Round 1 — Catalog & Price List Server Persistence

## What Shipped

### Rusty (Sonnet)

- **API Contract finalized:** `/api/price-list` and `/api/catalog` endpoints with version semantics, auth requirements, error codes.
- **Schema ready:** `price_list_versions`, `materials_catalog_versions` tables with full spec.
- **Quote table updated:** `price_list_version_id` column added (indexed FK).
- **Server boots:** Required `npm rebuild better-sqlite3` for Node 25.

### Livingston (Haiku)

- **Data Contract finalized:** All 16+ catalog categories mapped with field annotations (PERSIST vs. TRANSIENT).
- **Versioning strategy:** Full snapshot approach at quote save time (price list, catalog, labor rates, overhead rates).
- **Risk catalog:** 6 identified risks with mitigations (material string lookup, zero-price items, weight field, immutability, category coverage, overhangs).
- **⚠️ FLAGGED BUG:** Catalog `weight` field is all zeros but calculator uses it for structural-steel labor cost. Needs fix: add `weight_per_unit` to schema, derive calculated weight at calc time.

### Linus (Haiku)

- **Wiring map finalized:** 4 API client functions spec'd, context state changes documented, PriceListPage UI plan, quote save flow extended, offline fallback strategy.
- **Implementation checklist:** 12 items ready for code phase.
- **Risks identified:** Redux-style dispatch scalability, context init gating, version history UI (deferred to Phase 2).
- **No code yet:** Deferred pending Rusty's routes.

### Saul (Sonnet)

- **Test scaffold:** Vitest 4.1.5 added to webapp/package.json; vitest.config.ts created; 12 calculator tests written.
- **Test plan finalized:** Server endpoint tests spec'd (happy path, 401, 400, 404, 409 for all 10 endpoints); round-trip tests; webapp integration tests.
- **Test runner recommendation:** Node's built-in `node:test` + `supertest` for server (zero new deps, faster startup).
- **Tests not yet executed:** Deps not installed. Run `npm install` in webapp/ before testing.

## Critical Blocker: Weight Field Bug

Catalog `weight` field is all zeros. Structural-steel labor cost calculation relies on weight but gets $0. Under investigation by Livingston. Mitigation: add `weight_per_unit` (lb/unit) to catalog schema; calculator derives weight at runtime.

## Action Items for Jose

1. **Install dependencies:** Run `npm install` in `webapp/` to install vitest. Then `npm run test` to verify calculator tests pass.
2. **Weight field investigation:** Confirm whether weight is supposed to come from catalog or be calculated separately. If from catalog, Rusty needs to add schema field and backfill data.
3. **Next phase decision:** Phase 1 is lowest-risk, fastest to ship (API contract + schema + wiring map ready). Pick Phase 1 (server persistence) or Phase 2 (customers master data) to implement next.

## Decisions Merged

All 4 contracts merged to `.squad/decisions.md` for team consensus and reference.
